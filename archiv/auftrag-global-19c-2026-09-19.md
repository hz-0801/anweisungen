# Auftrag: Signalwort mit Ort, Stand 2026-09-19c

## Schritte

1. In `global.md` den Absatz, der mit „Signalwort:" beginnt,
   vollständig ersetzen durch:

   Signalwort: Muss ich selbst etwas tun – drücken, kopieren,
   einsetzen, freigeben –, beginnt die Zeile mit „Holger:" und nennt
   Ort und Handlung: welche App, welcher Reiter, welches Projekt oder
   welche Code-Sitzung, welches Repo, welche Datei. Braucht es mehrere
   Handgriffe, gibst du zuerst eine kurze nummerierte Übersicht ohne
   Signalwort, dann je Nachricht eine „Holger:"-Zeile, wartest auf die
   Rückmeldung, dann die nächste. Alles ohne dieses Wort ist
   Information, die kein Handeln verlangt.

   Die drei Zeichen Einrückung entfallen beim Einfügen.

2. In `global.md` beide Stand-Angaben von `2026-09-19b` auf
   `2026-09-19c` setzen (Kopfzeile und die Zeile `Stand:` unterhalb
   der Trennlinie).

3. Falls `kandidaten.md` den Block „## Übergabe verweist statt zu
   wiederholen" noch nicht enthält: am Ende anfügen, nach einer
   Leerzeile:

   ## Übergabe verweist statt zu wiederholen
   Regel: Die Übergabe nennt Entscheidungen und Befunde mit Verweis
   auf ihren dauerhaften Ort (konzept.md § 4, Profildateien) und
   wiederholt nur, was noch nirgends liegt. Was beim Umzug noch
   nicht abgelegt ist, wird als erster Auftrag des neuen Chats
   abgelegt. Der kurze Auftrag steht im Block zuerst, die lange
   Datei danach; kopiert wird mit dem Kopierknopf des Blocks.
   Grund: Übergabe 19c war 190 Zeilen, davon zwei Drittel
   Wiederholung.
   Herkunft: verbessereBlätter(), 2026-09-19.
   Reife: Kandidat

4. Diese Auftragsdatei nach
   `archiv/auftrag-global-19c-2026-09-19.md` verschieben.
5. Ein Commit: „Signalwort mit Ort (2026-09-19c)".

## Prüfungen

- `global.md` enthält „welche Code-Sitzung" genau einmal und
  `Stand: 2026-09-19c` genau einmal.
- `kandidaten.md` enthält „## Übergabe verweist" genau einmal.
- UTF-8, LF, kein BOM. Sonst nichts geändert.

## Bericht (zurück in den Chat)

- Commit-Hash. Letzte Zeile: „Push origin drücken".
